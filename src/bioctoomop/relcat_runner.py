def run_relcat(relcat, entities, sentences, note_id):
    """
    Placeholder – wire in RelCAT later.
    """
    relations = []
    relation_id = 0

    # Pseudocode – depends on your RelCAT wrapper
    # for rel in relcat.predict(...):
    #     relations.append({...})

    return relations
